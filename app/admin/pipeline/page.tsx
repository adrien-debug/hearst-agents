import CanvasShell from "../_canvas/CanvasShell";

export const dynamic = "force-dynamic";

/**
 * Canvas pipeline temps réel — déplacé depuis `/admin` pour laisser l’accueil
 * (`/admin`) comme vue d’ensemble du menu.
 */
export default function AdminPipelinePage() {
  return <CanvasShell />;
}
