# Projet Supabase : Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# LLM Providers
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# API Auth (leave empty to disable auth in dev)
HEARST_API_KEY=

# Google OAuth (Gmail read-only)
# 1. Create OAuth 2.0 credentials at https://console.cloud.google.com/apis/credentials
# 2. Enable Gmail API at https://console.cloud.google.com/apis/library/gmail.googleapis.com
# 3. Add redirect URI: http://localhost:9000/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
NEXTAUTH_SECRET=          # openssl rand -base64 32
NEXTAUTH_URL=http://localhost:9000
TOKEN_ENCRYPTION_KEY=     # openssl rand -hex 32

# Slack OAuth
# 1. Create app at https://api.slack.com/apps
# 2. Add redirect URI: http://localhost:9000/api/auth/callback/slack
# 3. Bot Token Scopes: channels:read, channels:history, im:read, im:history, users:read, groups:read, groups:history, mpim:read, mpim:history
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=

# Managed Agents (optional — cached after first creation)
# Set these to reuse existing agent/env across restarts
MANAGED_AGENT_ID=
MANAGED_ENV_ID=
